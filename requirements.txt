websocket-client==1.8.0
requests[socks]>=2.28.0  # SOCKS5 proxy cho fallback khi curl_cffi lỗi TLS
beautifulsoup4>=4.11.0
playwright>=1.40.0
curl_cffi>=0.5.0  # Nếu gặp lỗi TLS (35): pip install curl_cffi==0.7.4
